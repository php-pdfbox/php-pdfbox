checks:
  php:
    code_rating: true
    duplication: true

filter:
  excluded_paths:
    - Tests/*

coding_style:
  php:
    spaces:
      around_operators:
        concatenation: false
      before_left_brace:
        function: false
